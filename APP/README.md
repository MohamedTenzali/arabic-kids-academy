# APP

This folder is the future organized app structure for ArabicoKids.

The live app currently still runs from the repository root folders (`css`, `js`, `pages`, `audio`, `images`, `data`) so the website does not break while we reorganize. Move files here gradually when the HTML paths and service worker are updated together.
